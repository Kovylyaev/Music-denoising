defaults:
  - _self_
  - dataset: default
  - model: default
  - training: default

project:
  name: "Music_MMLS"
  seed: 42
  device: "cuda"

  wandb_entity: "sasha_kovylyaev-hse"
  experiment_name: "default"
